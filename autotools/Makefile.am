SUBDIRS = src
dist_doc_DATA = README

clean-local:
	rm config.status configure config.log
	rm Makefile
	rm -r autom4te.cache/
	rm aclocal.m4 depcomp
	rm compile install-sh missing Makefile.in
	rm src/Makefile src/Makefile.in
	rm -rf .deps src/.deps
